import { User } from './2-defining-own-types';

function getAge(user: User): number {
  // TODO
}

function getName(user: User): string {
  // TODO
}

function updateAge(user: User): void {
  user.age += 1;
}

// Satisfy TS
export {};
